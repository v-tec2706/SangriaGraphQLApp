package benchmark.entities

case class University(
                       id: Long,
                       cityId: Long,
                       name: String,
                       url: String
                     )
